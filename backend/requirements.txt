fastapi==0.115.0
uvicorn==0.30.6
python-dotenv==1.0.1
pydantic==2.11.7
email-validator==2.3.0
supabase==2.31.0
boto3==1.35.0
aiohttp==3.10.5
httpx==0.27.2
numpy>=1.26.4,<2.0
scipy==1.14.1
librosa==0.10.2
mido>=1.3.0
python-multipart==0.0.20
pypinyin==0.52.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2
sentry-sdk[fastapi,sqlalchemy]==2.42.0
soundfile>=0.12.1
pydub>=0.25.1
pillow>=10.0.0
# modal==1.5.3  # Step 4: 已从生产运行时移除，本地需 Modal 时手动 pip install modal==1.5.3

# --- MVP Kaggle 本地推理 (按需安装，生产镜像可不装) ---
# musicgen-small: transformers + accelerate (走 transformers 路径，不装 audiocraft 避免 torch 2.10 冲突)
# cosyvoice2 本阶段 BLOCKED，不安装（HF Space 亦 stub）
# Kaggle T4 中建议: pip install -r requirements-mvp.txt
transformers>=4.44.0
accelerate>=0.26.0
# audiocraft 不安装（可选，Kaggle 已验 transformers 路径可用）
# encodec==0.1.1 仅当显式需要 EnCodec 时启用
# cosyvoice>=0.1.0  # BLOCKED 本阶段
